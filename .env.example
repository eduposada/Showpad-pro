# Variáveis do Supabase (obrigatórias para login/sync):
# VITE_SUPABASE_URL=
# VITE_SUPABASE_ANON_KEY=

# Garimpo no localhost: não precisa de variável extra — o Vite serve POST /api/scrape na própria máquina.
