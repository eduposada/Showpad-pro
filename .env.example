# Copie para .env.local e ajuste. Reinicie `npm run dev` após alterar.
# O Garimpo chama POST /api/scrape; no dev o Vite repassa este pedido ao deploy.
VITE_API_SCRAPE_URL=https://seu-projeto.vercel.app
